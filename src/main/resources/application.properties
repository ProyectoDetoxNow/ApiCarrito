
spring.application.name=CarritoDeCompras
server.port=${PORT:8080}
server.address=0.0.0.0

# --- PostgreSQL connection ---
# spring.datasource.url=jdbc:postgresql://${DB_NEON_HOST2}:${DB_NEON_PORT2}/${DB_NEON_NAME2}?sslmode=require
# spring.datasource.username=${DB_NEON_ADMIN2}
# spring.datasource.password=${DB_NEON_PASS2}
# spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.url=jdbc:postgresql://ep-cool-star-admyzo4m-pooler.c-2.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_RoDB1GMLwsj5
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- Optional: timezone and encoding ---
spring.datasource.hikari.connection-init-sql=SET TIME ZONE 'UTC'
spring.sql.init.platform=postgres

